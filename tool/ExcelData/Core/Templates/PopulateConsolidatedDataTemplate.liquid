        internal static async Task Populate(SqlConnection connection)
        {
{% for table in model -%}
            await Populate_{{table}}_Data(connection).ConfigureAwait(false);
{% endfor -%}
        }
